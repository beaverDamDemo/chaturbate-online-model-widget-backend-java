spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/widgetdb
    username: widgetuser
    password: widgetpass
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

security:
  jwt:
    secret: 0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

admin:
  seed:
    email: fjasdojf@hotmail.com
    password: password
